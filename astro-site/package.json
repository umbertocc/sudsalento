{
  "name": "sudsalento-astro",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "deploy-local": "astro build && rd /s /q ..\\docs && mkdir ..\\docs && xcopy /s /e /y dist ..\\docs",
    "preview": "astro preview",
    "seo:audit:docs": "node scripts/seo-audit-docs.mjs ../docs",
    "images:optimize": "node scripts/optimize-images.mjs",
    "images:optimize:dry": "node scripts/optimize-images.mjs --dry-run",
    "images:webp": "node scripts/generate-webp.mjs",
    "images:webp:dry": "node scripts/generate-webp.mjs --dry-run"
  },
  "dependencies": {
    "@tailwindcss/vite": "^4.2.2",
    "astro": "^5.4.2"
  },
  "devDependencies": {
    "sharp": "^0.34.5"
  }
}
